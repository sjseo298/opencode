#!/usr/bin/env python3
"""Wrapper for opencodeplus — runs the main menu."""
import sys
from pathlib import Path

# Ensure scripts dir is on path so opencodeplus.py can be imported
SCRIPT_DIR = Path(__file__).parent.resolve()
sys.path.insert(0, str(SCRIPT_DIR))

from opencodeplus import main
main()
